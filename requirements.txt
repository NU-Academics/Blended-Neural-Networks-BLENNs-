# ============================================================================
# BLENNS ORIGINAL — Complete Trading Pipeline
# Requirements File
# ============================================================================
# Author: BLENNS Framework Implementation
# Date: April 2026
# Python: 3.8 - 3.11
# ============================================================================

# ============================================================================
# CORE DEPENDENCIES
# ============================================================================

# Data manipulation and analysis
numpy>=1.21.0,<1.25.0          # NumPy 1.25+ may have compatibility issues with TensorFlow
pandas>=1.3.0,<2.0.0           # Pandas 2.0 introduces API changes (pyarrow dependency)
scikit-learn>=1.0.0,<1.3.0     # For preprocessing (MinMaxScaler) and metrics

# ============================================================================
# DATA FETCHING
# ============================================================================

yfinance>=0.2.0,<0.3.0         # Yahoo Finance API (supports stocks, crypto, forex, futures)

# ============================================================================
# DEEP LEARNING FRAMEWORK
# ============================================================================

tensorflow>=2.10.0,<2.15.0     # TensorFlow 2.x with Keras (CNN + LSTM + Attention)
# Note: TensorFlow 2.15+ requires CUDA 12 and may break existing models

# ============================================================================
# VISUALIZATION
# ============================================================================

matplotlib>=3.5.0,<3.8.0       # Core plotting and candlestick charts
mplfinance>=0.12.0,<0.13.0     # Candlestick charting library (replaces mpl_finance)
Pillow>=9.0.0,<10.0.0          # Image processing for candle encoding
seaborn>=0.11.0,<0.13.0        # Statistical visualizations (optional but recommended)

# ============================================================================
# MODEL INTERPRETABILITY (XAI)
# ============================================================================

shap>=0.41.0,<0.43.0           # SHAP for model explainability (GradientExplainer)

# ============================================================================
# STATISTICAL ANALYSIS
# ============================================================================

scipy>=1.8.0,<1.10.0           # Statistical functions (norm, ttest, etc.)
statsmodels>=0.13.0,<0.15.0    # Advanced statistical models (ARIMA, GARCH)

# ============================================================================
# OPTIONAL BUT RECOMMENDED
# ============================================================================

# Jupyter notebook support (for running the original Colab notebook)
jupyter>=1.0.0,<2.0.0
ipywidgets>=7.6.0,<8.0.0       # Interactive widgets for notebook
ipykernel>=6.0.0,<7.0.0        # IPython kernel for Jupyter

# Progress bars and logging
tqdm>=4.64.0,<5.0.0            # For training progress visualization

# Image processing for advanced visualization
opencv-python>=4.5.0,<5.0.0    # For image enhancement (optional)

# ============================================================================
# DEVELOPMENT DEPENDENCIES (not required for runtime)
# ============================================================================

# Testing
pytest>=7.0.0,<8.0.0
pytest-cov>=4.0.0,<5.0.0
pytest-xdist>=3.0.0,<4.0.0     # Parallel test execution

# Code formatting and linting
black>=22.0.0,<24.0.0
flake8>=5.0.0,<6.0.0
isort>=5.10.0,<6.0.0
mypy>=0.990,<1.0.0
pylint>=2.15.0,<3.0.0

# Documentation
sphinx>=5.0.0,<7.0.0
sphinx-rtd-theme>=1.0.0,<2.0.0
nbsphinx>=0.8.0,<0.9.0         # Jupyter notebook support in Sphinx

# Pre-commit hooks
pre-commit>=2.20.0,<3.0.0

# ============================================================================
# PLATFORM-SPECIFIC NOTES
# ============================================================================

# For CUDA 11.2+ GPU acceleration (optional):
# tensorflow-gpu is included in tensorflow>=2.10.0 (no separate package needed)

# For Apple Silicon (M1/M2) Macs:
# Use tensorflow-macos and tensorflow-metal instead of standard tensorflow
# Uncomment below if using Apple Silicon:
# tensorflow-macos>=2.10.0,<2.15.0
# tensorflow-metal>=0.6.0,<1.0.0

# For Windows with limited RAM:
# Consider reducing batch size; no additional packages needed

# ============================================================================
# COMPATIBILITY MATRIX
# ============================================================================
# | Package          | Min Version | Max Version | Reason for upper bound          |
# |------------------|-------------|-------------|----------------------------------|
# | numpy            | 1.21.0      | <1.25.0     | TensorFlow 2.x compatibility     |
# | pandas           | 1.3.0       | <2.0.0      | API stability, no pyarrow issues |
# | tensorflow       | 2.10.0      | <2.15.0     | CUDA 11.x compatibility          |
# | shap             | 0.41.0      | <0.43.0     | API stability                    |
# | matplotlib       | 3.5.0       | <3.8.0      | mplfinance compatibility         |
# | scikit-learn     | 1.0.0       | <1.3.0      | API stability                    |
# | statsmodels      | 0.13.0      | <0.15.0     | ARIMA API stability              |
# | scipy            | 1.8.0       | <1.10.0     | Compatibility with statsmodels   |
# ============================================================================

# ============================================================================
# INSTALLATION COMMANDS
# ============================================================================

# Minimal installation (core only):
#   pip install numpy pandas yfinance tensorflow matplotlib mplfinance Pillow scikit-learn shap

# Standard installation (with optional dependencies):
#   pip install -r requirements.txt

# Development installation (with testing and docs):
#   pip install -r requirements-dev.txt

# GPU installation (CUDA 11.2+):
#   pip install tensorflow>=2.10.0,<2.15.0  (includes GPU support automatically)

# Apple Silicon installation:
#   pip install tensorflow-macos tensorflow-metal
#   pip install numpy pandas yfinance matplotlib mplfinance Pillow scikit-learn shap

# ============================================================================
# VERIFICATION
# ============================================================================
# After installation, verify with:
#   python -c "import tensorflow as tf; print(tf.__version__)"
#   python -c "import shap; print(shap.__version__)"
#   python -c "import yfinance; print(yfinance.__version__)"
# ============================================================================
